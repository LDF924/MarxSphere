# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| main 分支 | ✅ 积极维护 |
| 历史版本 | ❌ 不提供安全补丁（建议使用最新 main） |

## 报告安全漏洞

MarxSphere 重视安全。发现安全漏洞请通过以下方式报告（**不要**在公开 issue 中披露）：

1. **GitHub 私有漏洞报告**（推荐）：仓库页面 → Security → Report a vulnerability
2. **邮箱**：fudeng69@gmail.com（回复时效：3 个工作日内）
3. **备用邮箱**：2665834886@qq.com（fudeng69@gmail.com 不可达时使用）

请在报告中包含：
- 漏洞类型（XSS/SQL 注入/SSRF/权限绕过/密钥泄露等）
- 受影响版本与文件路径
- 复现步骤（或 PoC）
- 影响范围与可能的危害

## 处理流程

1. 收到报告 → 48 小时内确认
2. 评估影响 → 严重漏洞 7 天内修复并发布
3. 修复后通知报告者致谢（除非要求匿名）

## 安全特性（本项目已有）

- **日志脱敏**：所有日志输出经 `sanitizeLine/sanitizeObject` 脱敏，API 密钥/个人信息不进日志
- **凭证隔离**：API Key 脱敏存储（sk-\*\*\*\*），沙箱环境剔除凭据
- **外部请求脱敏**：错误信息不暴露内部细节
- **Agent 安全**：Guardian 策略 / 3 级沙箱 / SSRF 防护 / 审批门 / 工具黑名单
- **.env 防护**：密钥文件永不入库（.gitignore 强制）
- **依赖安全**：建议使用 `npm audit` 定期检查依赖

## 已知边界（设计如此，非漏洞）

- 本地单机部署模式下 `localhost` 豁免鉴权（外部连接强制 Bearer Token 鉴权已硬编码生效；`AUTH_MODE` 控制用户注册/登录模式，生产暴露前建议设置强 `JWT_SECRET`）
- 上传的文献内容由使用者负责版权合规
